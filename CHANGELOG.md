# Changelog

## Unreleased

- Function-level `crap-line-v1` engine with explicit LLVM/xccov ownership and deterministic gates.
- Project, package, target, explicit manifest, file and resolved Xcode target selection.
- Capture receipts binding source inputs, coverage artifacts, command and compiler-aware callable inventory.
- Mandatory provenance for CLI analysis unless an unverified import is explicitly requested.
- Compiler-backed conditional compilation and experimental parser feature selection.
- Native LLVM integration, genuine Xcode result bundles, executable acceptance tests and pinned upstream compatibility audits.

The executable currently reports `1.0.0`; no public release tag has been published. Breaking pre-release workflow changes are described in the contract and README.
